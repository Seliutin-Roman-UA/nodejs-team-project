// const News = require("../schemas/newsSchema");

// const getNews = async (req, res, next) => {
//   const newsAll = await News.find({});
//   res.status(200).send(newsAll);
// };

// module.exports = getNews;
